# Renderer Configuration

Resume content lives only in [`../resume.json`](../resume.json). This directory contains
renderer-specific configuration that does not belong in the portable resume contract.

- `themes.json` defines RxResume layout and presentation.

Do not add profile content or generated resume variants here.
